// https://leetcode-cn.com/problems/burst-balloons/
template <typename T>
class Matrix
{
private:
    T* _data;
public:
    Matrix(unsigned rowCount, unsigned colCount)
        : _data(new T[rowCount * colCount]) // 应该用 allocate
    { }

    
};
class Solution 
{
public:
    int maxCoins(vector<int>& nums) 
    {
        
    }
};